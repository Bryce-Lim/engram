#!/usr/bin/env python3
"""Executable entry point for the Precog CLI.

Adds the project root to sys.path so ``precog`` runs from a checkout without
installation, then delegates to :func:`precog.cli.main`.
"""

import os
import sys

_HERE = os.path.dirname(os.path.abspath(__file__))
_ROOT = os.path.dirname(_HERE)
if _ROOT not in sys.path:
    sys.path.insert(0, _ROOT)

from precog.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
